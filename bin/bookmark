#!/usr/bin/env bash

if [[ ! "$@" ]]; then
  selected="$(cat ~/notes/more/bookmarks | uniq | fzf)"
  echo "$selected"
  echo "$selected" | xclip -selection clipboard
  # xdg-open "$selected" &
else
  echo "$@" >> ~/notes/more/bookmarks
fi

