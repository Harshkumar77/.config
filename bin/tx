#!/usr/bin/env bash

session=`tmux ls -F \#S | fzf`
(tmux -u switch -t "$session" || tmux -u attach -t "$session" ) || 
( 
    echo "No existing session. Creating new one" | lolcat;
    read -p "Name : " sname;
    tmux -u new-session -s "$sname";
)


