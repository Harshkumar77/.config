#!/usr/bin/env bash

tmux rename-window "session selector"
session=`tmux ls -F \#S | fzf`
(tmux switch -t "$session" || tmux attach -t "$session" ) || 
( 
    echo "No existing session. Creating new one" | lolcat;
    read -p "Name : " sname;
    tmux new-session -s "$sname";
)


